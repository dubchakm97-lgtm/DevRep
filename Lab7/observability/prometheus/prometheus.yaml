global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "flask-metrics-app"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "flask-service.default.svc.cluster.local:80"
